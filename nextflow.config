/// docker.enabled = true
process.container = 'nriviera/bwa-nf-test'
docker.runOptions='-u $(id -u):$(id -g)'


// Profiles
profiles {
  cluster {
    includeConfig 'conf/cluster.config'
  }
  test{
    includeConfig 'conf/test.config'
  }
}
